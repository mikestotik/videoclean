{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "runpod": {
      "type": "local",
      "command": ["npx", "-y", "@runpod/mcp-server@latest"],
      "environment": {
        "RUNPOD_API_KEY": "{env:RUNPOD_API_KEY}"
      }
    }
  }
}
