name: ci

on:
  push:
    branches: [main]
  pull_request:

jobs:
  python:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: astral-sh/setup-uv@v6
        with:
          enable-cache: true
      - name: Sync + test
        run: |
          uv sync --extra web --extra lama --no-dev
          uv run pytest -q

  webui:
    # DISABLED for the test loop (backend only): remove `if: false` to re-enable.
    if: false
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: webui
    steps:
      - uses: actions/checkout@v5
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
      - run: bun install --frozen-lockfile
      - run: bun run lint
      - run: bun run typecheck

  docker-web:
    # DISABLED for the test loop (backend only): remove `if: false` to re-enable.
    if: false
    runs-on: ubuntu-latest
    needs: [webui]
    steps:
      - uses: actions/checkout@v5
      # API image is CUDA-heavy; build locally / on GPU runners. Web image is the light CI check.
      - name: Build web image (no push)
        run: docker build -f Dockerfile.web -t videoclean-web:ci --build-arg VITE_API_BASE_URL=http://localhost:7860 .
